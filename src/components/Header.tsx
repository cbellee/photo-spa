/**
 * Header — Kept for backwards compatibility but no longer rendered
 * in the Layout (navigation moved to Sidebar). This component is 
 * still available if needed elsewhere.
 */
import React from "react";

export default function Header() {
  return null;
}
