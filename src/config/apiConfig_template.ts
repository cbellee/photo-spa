export const apiConfig = {
    azureTenantId: "{{AZURE_TENANT_ID}}",
    storageApiEndpoint: "https://{{STORAGE_ACCOUNT_NAME}}.{{STORAGE_ACCOUNT_SUFFIX}}",
    photoApiEndpoint: "https://{{PHOTO_APP_ENDPOINT_URI}}/api"
}
